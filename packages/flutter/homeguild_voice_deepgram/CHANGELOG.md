## 0.1.0

- Initial release
- `DeepgramSTTProvider` — real-time speech-to-text via Deepgram WebSocket API
- Multi-language support (`multi` mode for English/Spanish auto-detection)
- Voice Activity Detection (VAD) events
- Configurable model, language, encoding, and sample rate
- Keep-alive mechanism for stable WebSocket connections
- Optional usage tracking callback
